<li>
  <span class="event_name">
    <%= link_to event.name, event %>
  </span>
  <span class="event_orga">
    <% if not event.event_helpers.find_by_user_id(current_user.id).nil? and event.event_helpers.find_by_user_id(current_user.id).orga? %>
    (<%= t('event_orga') %>)
    <% else  %>
    (<%= t('event_helper') %>)
    <% end %>
  </span><br/>
  <%= raw  display_time_event(event) %><br/>
</li>
