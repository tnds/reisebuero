<%= form_for(@lodging) do |f| %>
  <% if @lodging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lodging.errors.count, "error") %> prohibited this lodging from being saved:</h2>

      <ul>
      <% @lodging.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= hidden_field(:lodging, :event_id) unless @lodging.event.nil? %>
  <%= hidden_field(:lodging, :request) %>
<p>
  <b>
    <%= t('event')+':' %>
  </b>
  <%= link_to @lodging.event.name, event_path(@lodging.event) unless @lodging.event.nil? %>
</p>

  <div class="field">
    <%= f.label :start_at %><br />
    <%= f.datetime_picker :start_at %>
  </div>
  <div class="field">
    <%= f.label :end_at %><br />
    <%= f.datetime_picker :end_at %>
  </div>
  <div class="field">
    <%= f.label :max_people %><br />
    <%= f.number_field :max_people %>
  </div>
  <div class="field">
    <%= f.label :accessible %><br />
    <%= f.check_box :accessible %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_area :location %>
  </div>
  <div class="field">
    <%= f.label :maplink %><br />
    <%= f.text_field :maplink %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
