<%= form_for(@contacttype) do |f| %>
  <% if @contacttype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contacttype.errors.count, "error") %> prohibited this contacttype from being saved:</h2>

      <ul>
      <% @contacttype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
